spring:
  #Configuracion para el datasource
  datasource:
    url: jdbc:h2:file:c:/h2/bbddSpringData
    username: sa
    password:

  #Configuracion para JPA
  jpa:
    open-in-view: false    
    show-sql: true
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
