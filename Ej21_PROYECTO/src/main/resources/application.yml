server:
  port: 10000

spring:
  application:
    name: "ServicioPeliculas"

  datasource:
    url: jdbc:h2:file:c:/h2/bbdd_TROLOLO
    driverClassName: org.h2.Driver
    username: sa
    password: 
        
  jpa: 
    open-in-view: false
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect        
    hibernate:
      ddl-auto: update
